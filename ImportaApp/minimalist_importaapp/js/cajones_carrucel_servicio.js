/* $(document).ready(function () {
    $('.slider').slick({
        dots: true,
        infinite: true,
        speed: 500,
        slidesToShow: 1,
        variableWidth: false, // Cambiado a false para deshabilitar el ancho variable
        adaptiveHeight: false, // Cambiado a false para deshabilitar la altura adaptativa
        autoplay: true,
        autoplaySpeed: 2000
    });
});
 */